#pragma once
class User 
{
public : 

protected : 
    string _nickname;
    string _password;

    void checkUser(nickname, password);
    void createUser(nickname, password);
  

};

